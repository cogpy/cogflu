# OpenCog Python dependencies for Influent integration
# Install with: pip install -r requirements.txt

# Core OpenCog packages (if available via pip)
# Note: OpenCog may need to be built from source for full functionality
opencog==5.0.3

# Alternative: Use mock implementation if OpenCog not available
# The Python bridge will fall back to mock mode automatically

# Supporting libraries for cognitive computing
numpy>=1.21.0
scipy>=1.7.0
networkx>=2.6.0
matplotlib>=3.4.0

# For graph analysis and pattern recognition
scikit-learn>=1.0.0
pandas>=1.3.0

# For distributed computing integration
dask>=2021.10.0

# Utilities
python-dateutil>=2.8.0
requests>=2.25.0