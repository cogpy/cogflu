# POM Audit - Maven Build Workflow for aperture-icons

name: Maven Build - aperture-icons

on:
  push:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'aperture-icons/**'
      - 'aperture-spi/**'
      - 'aperture-common/**'
      - 'aperture-geo/**'
      - 'pom.xml'
  pull_request:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'aperture-icons/**'
      - 'aperture-spi/**'
      - 'aperture-common/**'
      - 'aperture-geo/**'
      - 'pom.xml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build aperture-icons
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build aperture-icons with dependencies
      run: mvn -B clean install -DskipTests -pl aperture-spi,aperture-common,aperture-geo,aperture-icons -am
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Run tests for aperture-icons
      run: mvn -B test -pl aperture-icons
      continue-on-error: false
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Generate dependency tree
      run: mvn dependency:tree -DoutputFile=aperture-icons/dependency-tree.txt -pl aperture-icons
      continue-on-error: true
      
    - name: Upload dependency tree
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: dependency-tree-aperture-icons
        path: aperture-icons/dependency-tree.txt
        retention-days: 30