# POM Audit - Maven Build Workflow for aperture-capture-phantom

name: Maven Build - aperture-capture-phantom

on:
  push:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'aperture-capture-phantom/**'
      - 'aperture-spi/**'
      - 'aperture-common/**'
      - 'pom.xml'
  pull_request:
    branches: [ "master", "main", "develop" ]
    paths:
      - 'aperture-capture-phantom/**'
      - 'aperture-spi/**'
      - 'aperture-common/**'
      - 'pom.xml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build aperture-capture-phantom
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build aperture-capture-phantom with dependencies
      run: mvn -B clean install -DskipTests -pl aperture-spi,aperture-common,aperture-capture-phantom -am
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Run tests for aperture-capture-phantom
      run: mvn -B test -pl aperture-capture-phantom
      continue-on-error: true
      env:
        MAVEN_OPTS: '-Xmx2048m -Xms1024m'
        
    - name: Generate dependency tree
      run: mvn dependency:tree -DoutputFile=aperture-capture-phantom/dependency-tree.txt -pl aperture-capture-phantom
      continue-on-error: true
      
    - name: Upload dependency tree
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: dependency-tree-aperture-capture-phantom
        path: aperture-capture-phantom/dependency-tree.txt
        retention-days: 30